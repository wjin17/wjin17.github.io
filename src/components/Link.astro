---
interface Props {
  href: string;
  external?: boolean;
}

const { href, external = false } = Astro.props;
const linkProps = external
  ? { target: "_blank", rel: "noopener noreferrer" }
  : {};
---

<a
  href={href}
  {...linkProps}
  class="text-foreground hover:underline hover:text-foreground/60 transition-colors duration-300 ease-in-out"
>
  <slot />
</a>
